"""Recorded-event reader (deferred — M2/M4).

Reads Contract #1 ``RecordedEvent`` records back in ``(recording_run_id,
local_sequence)`` order and feeds them through the *same* decode boundary as
live. Stub only.
"""

__all__: list[str] = []
