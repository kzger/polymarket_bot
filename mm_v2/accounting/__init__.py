"""WS-C — accounting (no creds).

Fill-ledger → inventory bridge, three-bucket P&L (core / rebate / reward), and
reference + executable-unwind markout. Consumes Contract #1b parsed events and
WS-B domain types (PLAN §3.9, §7, §9).
"""

__all__: list[str] = []
