"""M0 conformance entry point (CREDENTIALED, gated).

Proves the full sign → submit → cancel → split → merge chain on the real
wallet/SDK and outputs the latency distribution that M0.5 calibrates against
(PLAN §8, §9 M0). Do not run without explicit M0 authorization + credentials.
Stub only.
"""

__all__: list[str] = []


def main() -> None:  # pragma: no cover - entry point stub
    raise NotImplementedError("M0 conformance harness is gated on credentials.")
