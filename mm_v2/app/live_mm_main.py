"""Live market-making entry point (CREDENTIALED, gated — M2.5+).

The tiny live canary: pre-split inventory, quote two economic directions
post-only, with heartbeat + GTD + stale-kill + max-unmatched guards (PLAN §9
M2.5). Do not run without explicit authorization + credentials. Stub only.
"""

__all__: list[str] = []


def main() -> None:  # pragma: no cover - entry point stub
    raise NotImplementedError("Live MM entry point is gated on credentials + M2.5.")
