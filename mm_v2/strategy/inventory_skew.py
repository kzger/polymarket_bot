"""Inventory skew (deferred — M3+).

Skews quotes against current directional inventory ``D`` to mean-revert toward a
target. Reads the Contract #3 ``shadow_owned_total`` basis via the inventory
accessors, never raw fields. Stub only.
"""

__all__: list[str] = []
